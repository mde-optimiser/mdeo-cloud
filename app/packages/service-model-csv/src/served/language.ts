/**
 * Re-exports the model-csv language plugin provider for the language server.
 * This file is served as an ESM module for the workbench language server to import.
 */
import { modelCsvPluginProvider } from "@mdeo/language-model-csv";

export default modelCsvPluginProvider;
